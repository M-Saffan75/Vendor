import { COLOURS } from "./theme";

const apptheme = {
    light: {
        theme: 'light',
        color: '#000',
        background: COLOURS.bluewhite,
        borderColor: '#000'
    },

    dark: {
        theme: 'dark',
        color: '#fff',
        background: '#1b1b1b',
        borderColor: '#fff'
    },
}

export default apptheme;